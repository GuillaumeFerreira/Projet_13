version: 2.1

# Define the jobs we want to run for this project
jobs:
  test:
    docker:
      - image: cimg/python:3.9.6
        auth:
          username: guillaumeferreira
          password: $DOCKERHUB_PASSWORD  # context / project UI env-var reference
    steps:
      - checkout
      - run: pip install pytest
      - run: pytest

workflows:
  test:
    jobs:
      - test